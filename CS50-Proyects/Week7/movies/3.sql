-- Selects the 'title' column from the 'movies' table.
-- Filters for movies with a release year on or after 2018.
-- Orders the results alphabetically by title.
SELECT title FROM movies WHERE year >= 2018 ORDER BY title;
